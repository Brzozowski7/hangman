import styled from "styled-components";
import { pallete } from "../../../misc/pallete";

export const Wrapper = styled.div`
  width: 30px;
  height: 40px;
  border-bottom: 2px solid ${pallete.Black};
  font-size: 30px;
  text-transform: uppercase;
  text-align: center;
`;
